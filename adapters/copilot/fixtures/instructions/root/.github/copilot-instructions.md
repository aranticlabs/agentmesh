# Repository Coding Instructions

Prefer small, reviewable changes that keep generated code consistent with the
existing module boundaries. When changing APIs, update call sites and tests in
the same change.

Use precise error messages that name the failing input or file path when that
information is safe to expose.
